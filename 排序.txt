#include <stdlib.h>
#include <time.h>
#include <stdio.h>
int main()
{
	srand(time(0));
	int a[10];
	for (int i = 0; i < 10; ++i)//随机数
	{
		a[i] = rand()%100;
	}
	/*
	//插入排序
	for (int i = 1; i < 10; ++i)
	{
		int t = a[i];
		int j;
		for (j = i - 1; j >= 0; --j)
		{
			if (t < a[j])
			{
				a[j + 1] = a[j];
			}
			else
			{				
				break;
			}
		}
		a[j + 1] = t;
	}
	*/
	/*
	//选择排序
	for (int j = 0; j < 10 - 1; ++j)
	{
		int k = j;
		for (int i = j; i < 10; ++i)
		{
			if (a[i] < a[k])
			{
				k = i;
			}
		}
		int t = a[j];
		a[j] = a[k];
		a[k] = t;
	}
	*/
	/*
	//冒泡排序
	for (int k = 0; k < 10 - 1; ++k)
	{
		for (int j = 0; j < 10 - 1 - k; ++j)
		{
			if (a[j] > a[j + 1])
			{
				int t = a[j];
				a[j] = a[j + 1];
				a[j + 1] = t;
			}
		}
		
	}
	*/
	for (int i = 0; i < 10; ++i)
	{
		printf("%d ", a[i]);
	}
	printf("\n");
	return 0;
}